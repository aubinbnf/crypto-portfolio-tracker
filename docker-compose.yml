services:
  crypto-tracker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENV=development
    volumes:
      - portfolio-data:/app/data

volumes:
  portfolio-data:
    driver: local